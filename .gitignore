# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Captured gameplay: replays and frames are large and regenerable.
# Manifests and experiment records are NOT ignored -- they are the evidence.
data/replays/
data/frames/
*.mp4
*.npz
*.npy

# Model checkpoints
checkpoints/
*.pt
*.ckpt

# Local overrides
.env
.claude/settings.local.json

# Persistent local latent banks (run evidence remains in experiments/).
data/memory/
